<UserControl xmlns="https://github.com/avaloniaui"
		xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
		xmlns:vm="using:Telidraw.ViewModels"
		xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
		xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
		xmlns:views="clr-namespace:Telidraw.Views"
		xmlns:i="using:Telidraw.Icons"
		xmlns:converters="clr-namespace:Telidraw.Converters"
		xmlns:Media="using:Avalonia.Media"
		mc:Ignorable="d" d:DesignWidth="640" d:DesignHeight="200"
	
		x:Class="Telidraw.Views.AddCommandView"
		x:DataType="vm:AddCommandViewModel">
	<Grid RowDefinitions="Auto,*" ColumnDefinitions="*">
		<!-- Top Section: ComboBox -->
		<ComboBox
			Grid.Row="0"
			HorizontalAlignment="Stretch"
			ItemsSource="{Binding Commands}"
			SelectedItem="{Binding SelectedCommand}">
			<ComboBox.ItemTemplate>
				<DataTemplate>
					<TextBlock Text="{Binding Name}" />
				</DataTemplate>
			</ComboBox.ItemTemplate>
		</ComboBox>

		<!-- Bottom Section: Command-specific Inputs -->
		<TextBlock Grid.Row="1" Text="{Binding SelectedCommand}" HorizontalAlignment="Center" VerticalAlignment="Center" />
	</Grid>
</UserControl>
